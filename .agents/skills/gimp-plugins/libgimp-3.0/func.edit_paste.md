![](../images/gimp-devel-logo.png)

### [Gimp](index.html)

API Version: 3.0

Library Version: 3.0.8

##### Functions

Generated by [gi-docgen](https://gitlab.gnome.org/GNOME/gi-docgen) 2023.1

### Function

# [Gimp](index.html)edit\_paste

#### Declaration [[src]](https://gitlab.gnome.org/GNOME/gimp/-/blob/master/libgimp/gimpedit_pdb.h#L38 "go to source location")

```
```
GimpDrawable**
gimp_edit_paste (
  GimpDrawable* drawable,
  gboolean paste_into
)
```
```

#### Description [[src]](https://gitlab.gnome.org/GNOME/gimp/-/blob/master/libgimp/gimpedit_pdb.c#L165 "go to source location")

Paste buffer to the specified drawable.

This procedure pastes a copy of the internal GIMP edit buffer to the
specified drawable. The GIMP edit buffer will be empty unless a call
was previously made to either [`gimp_edit_cut()`](func.edit_cut.html) or
[`gimp_edit_copy()`](func.edit_copy.html). The "paste\_into" option specifies whether
to clear the current image selection, or to paste the buffer
"behind" the selection. This allows the selection to act as a mask
for the pasted buffer. Anywhere that the selection mask is non-zero,
the pasted buffer will show through. The pasted data may be a
floating selection when relevant, layers otherwise. If the image has
a floating selection at the time of pasting, the old floating
selection will be anchored to its drawable before the new floating
selection is added.
This procedure returns the new drawables (floating or not). If the
result is a floating selection, it will already be attached to the
specified drawable, and a subsequent call to
[`gimp_floating_sel_attach()`](func.floating_sel_attach.html) is not needed.

#### Parameters

`drawable`
:   *Type:* [`GimpDrawable`](class.Drawable.html)

    The drawable to paste to.

    |  |
    | --- |
    | The data is owned by the caller of the function. |

`paste_into`
:   *Type:* `gboolean`

    Clear selection, or paste behind it?

#### Return value

*Type:* An array of `None`

```
     The list of pasted layers.
     The returned value must be freed with g_free().
```

|  |
| --- |
| The array is `NULL`-terminated. |
| The caller of the function takes ownership of the data container, but not the data inside it. |
