![](../images/gimp-devel-logo.png)

### [Gimp](index.html)

API Version: 3.0

Library Version: 3.0.8

##### Functions

Generated by [gi-docgen](https://gitlab.gnome.org/GNOME/gi-docgen) 2023.1

### Function

# [Gimp](index.html)enum\_get\_value

since: 2.2

#### Declaration [[src]](https://gitlab.gnome.org/GNOME/gimp/-/blob/master/libgimpbase/gimputils.h#L51 "go to source location")

```
```
gboolean
gimp_enum_get_value (
  GType enum_type,
  gint value,
  const gchar** value_name,
  const gchar** value_nick,
  const gchar** value_desc,
  const gchar** value_help
)
```
```

#### Description [[src]](https://gitlab.gnome.org/GNOME/gimp/-/blob/master/libgimpbase/gimputils.c#L743 "go to source location")

Checks if `value` is valid for the enum registered as `enum_type`.
If the value exists in that enum, its name, nick and its translated
description and help are returned (if `value_name`, `value_nick`,
`value_desc` and `value_help` are not `NULL`).

Available since: 2.2

#### Parameters

`enum_type`
:   *Type:* `GType`

    The `GType` of a registered enum.

`value`
:   *Type:* `gint`

    An integer value.

`value_name`
:   *Type:* `const gchar**`

    Return location for the value’s name, or `NULL`.

    |  |
    | --- |
    | The argument will be set by the function. |
    | The argument can be `NULL`. |
    | The called function takes ownership of the data, and is responsible for freeing it. |
    | The value is a NUL terminated UTF-8 string. |

`value_nick`
:   *Type:* `const gchar**`

    Return location for the value’s nick, or `NULL`.

    |  |
    | --- |
    | The argument will be set by the function. |
    | The argument can be `NULL`. |
    | The called function takes ownership of the data, and is responsible for freeing it. |
    | The value is a NUL terminated UTF-8 string. |

`value_desc`
:   *Type:* `const gchar**`

    Return location for the value’s translated
    description, or `NULL`.

    |  |
    | --- |
    | The argument will be set by the function. |
    | The argument can be `NULL`. |
    | The called function takes ownership of the data, and is responsible for freeing it. |
    | The value is a NUL terminated UTF-8 string. |

`value_help`
:   *Type:* `const gchar**`

    Return location for the value’s translated
    help, or `NULL`.

    |  |
    | --- |
    | The argument will be set by the function. |
    | The argument can be `NULL`. |
    | The called function takes ownership of the data, and is responsible for freeing it. |
    | The value is a NUL terminated UTF-8 string. |

#### Return value

*Type:* `gboolean`

`TRUE` if `value` is valid for the `enum_type`, `FALSE` otherwise.
