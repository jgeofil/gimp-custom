![](../images/gimp-devel-logo.png)

### [Gimp](index.html)

API Version: 3.0

Library Version: 3.0.8

##### Functions

Generated by [gi-docgen](https://gitlab.gnome.org/GNOME/gi-docgen) 2023.1

### Function

# [Gimp](index.html)data\_directory

#### Declaration [[src]](https://gitlab.gnome.org/GNOME/gimp/-/blob/master/libgimpbase/gimpenv.h#L51 "go to source location")

```
```
const gchar*
gimp_data_directory (
  void
)
```
```

#### Description [[src]](https://gitlab.gnome.org/GNOME/gimp/-/blob/master/libgimpbase/gimpenv.c#L510 "go to source location")

Returns the default top directory for GIMP data. If the environment
variable GIMP3\_DATADIR exists, that is used. It should be an
absolute pathname. Otherwise, on Unix the compile-time defined
directory is used. On Windows, the installation directory as
deduced from the executable’s full filename is used.

Note that the actual directories used for GIMP data files can be
overridden by the user in the preferences dialog.

In config files such as gimprc, the string ${gimp\_data\_dir} expands
to this directory.

The returned string is owned by GIMP and must not be modified or
freed. The returned string is in the encoding used for filenames by
GLib, which isn’t necessarily UTF-8. (On Windows it always is
UTF-8.)

#### Return value

*Type:* `const gchar*`

The top directory for GIMP data.

|  |
| --- |
| The data is owned by the called function. |
| The value is a NUL terminated UTF-8 string. |
