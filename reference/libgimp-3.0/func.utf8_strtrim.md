![](../images/gimp-devel-logo.png)

### [Gimp](index.html)

API Version: 3.0

Library Version: 3.0.8

##### Functions

Generated by [gi-docgen](https://gitlab.gnome.org/GNOME/gi-docgen) 2023.1

### Function

# [Gimp](index.html)utf8\_strtrim

#### Declaration [[src]](https://gitlab.gnome.org/GNOME/gimp/-/blob/master/libgimpbase/gimputils.h#L29 "go to source location")

```
```
gchar*
gimp_utf8_strtrim (
  const gchar* str,
  gint max_chars
)
```
```

#### Description [[src]](https://gitlab.gnome.org/GNOME/gimp/-/blob/master/libgimpbase/gimputils.c#L88 "go to source location")

Creates a (possibly trimmed) copy of `str`. The string is cut if it
exceeds `max_chars` characters or on the first newline. The fact
that the string was trimmed is indicated by appending an ellipsis.

#### Parameters

`str`
:   *Type:* `const gchar*`

    An UTF-8 encoded string (or `NULL`)

    |  |
    | --- |
    | The argument can be `NULL`. |
    | The data is owned by the caller of the function. |
    | The value is a NUL terminated UTF-8 string. |

`max_chars`
:   *Type:* `gint`

    The maximum number of characters before the string get trimmed.

#### Return value

*Type:* `gchar*`

A (possibly trimmed) copy of `str` which should be freed
using `g_free()` when it is not needed any longer.

|  |
| --- |
| The caller of the function takes ownership of the data, and is responsible for freeing it. |
| The value is a NUL terminated UTF-8 string. |
