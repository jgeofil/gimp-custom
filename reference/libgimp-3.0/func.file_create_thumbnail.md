![](../images/gimp-devel-logo.png)

### [Gimp](index.html)

API Version: 3.0

Library Version: 3.0.8

##### Functions

Generated by [gi-docgen](https://gitlab.gnome.org/GNOME/gi-docgen) 2023.1

### Function

# [Gimp](index.html)file\_create\_thumbnail

#### Declaration [[src]](https://gitlab.gnome.org/GNOME/gimp/-/blob/master/libgimp/gimpfile_pdb.h#L47 "go to source location")

```
```
gboolean
gimp_file_create_thumbnail (
  GimpImage* image,
  GFile* file
)
```
```

#### Description [[src]](https://gitlab.gnome.org/GNOME/gimp/-/blob/master/libgimp/gimpfile_pdb.c#L216 "go to source location")

Creates a thumbnail of `image` for the given `file`

This procedure creates a thumbnail for the given `file` and stores it
according to relevant standards.
In particular, it will follow the [Free Desktop Thumbnail Managing
Standard](https://specifications.freedesktop.org/thumbnail-spec/latest/thumbsave.html) when relevant.

The thumbnail is stored so that it belongs to the given `file`. This
means you have to save `image` under this name first. As a fallback,
the call will work if `image` was exported or imported as `file`. In
any other case, this procedure will fail.

#### Parameters

`image`
:   *Type:* [`GimpImage`](class.Image.html)

    The image.

    |  |
    | --- |
    | The data is owned by the caller of the function. |

`file`
:   *Type:* `GFile`

    The file the thumbnail belongs to.

    |  |
    | --- |
    | The data is owned by the caller of the function. |

#### Return value

*Type:* `gboolean`

TRUE on success.
