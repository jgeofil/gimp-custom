![](../images/gimp-devel-logo.png)

### [Gimp](index.html)

API Version: 3.0

Library Version: 3.0.8

##### Functions

Generated by [gi-docgen](https://gitlab.gnome.org/GNOME/gi-docgen) 2023.1

### Function

# [Gimp](index.html)file\_get\_utf8\_name

since: 2.10

#### Declaration [[src]](https://gitlab.gnome.org/GNOME/gimp/-/blob/master/libgimpbase/gimputils.h#L37 "go to source location")

```
```
const gchar*
gimp_file_get_utf8_name (
  GFile* file
)
```
```

#### Description [[src]](https://gitlab.gnome.org/GNOME/gimp/-/blob/master/libgimpbase/gimputils.c#L280 "go to source location")

This function works like `gimp_filename_to_utf8()` and returns
a UTF-8 encoded string that does not need to be freed.

It converts a `GFile`‘s path or uri to UTF-8 temporarily. The
return value is a pointer to a string that is guaranteed to be
valid only during the current iteration of the main loop or until
the next call to gimp\_file\_get\_utf8\_name().

The only purpose of this function is to provide an easy way to pass
a `GFile`‘s name to a function that expects an UTF-8 encoded string.

See g\_file\_get\_parse\_name().

Available since: 2.10

#### Parameters

`file`
:   *Type:* `GFile`

    A `GFile`.

    |  |
    | --- |
    | The data is owned by the caller of the function. |

#### Return value

*Type:* `const gchar*`

A temporarily valid UTF-8 representation of `file`‘s name.
This string must not be changed or freed.

|  |
| --- |
| The data is owned by the called function. |
| The value is a NUL terminated UTF-8 string. |
