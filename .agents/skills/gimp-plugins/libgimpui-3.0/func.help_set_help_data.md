![](../images/gimp-devel-logo.png)

### [GimpUi](index.html)

API Version: 3.0

Library Version: 3.0.8

##### Functions

Generated by [gi-docgen](https://gitlab.gnome.org/GNOME/gi-docgen) 2023.1

### Function

# [GimpUi](index.html)help\_set\_help\_data

#### Declaration [[src]](https://gitlab.gnome.org/GNOME/gimp/-/blob/master/libgimpwidgets/gimphelpui.h#L48 "go to source location")

```
```
void
gimp_help_set_help_data (
  GtkWidget* widget,
  const gchar* tooltip,
  const gchar* help_id
)
```
```

#### Description [[src]](https://gitlab.gnome.org/GNOME/gimp/-/blob/master/libgimpwidgets/gimphelpui.c#L170 "go to source location")

The reason why we don’t use `gtk_widget_set_tooltip_text()` is that
elements in the GIMP user interface should, if possible, also have
a `help_id` set for context-sensitive help.

This function can be called with `NULL` for `tooltip`. Use this feature
if you want to set a help link for a widget which shouldn’t have
a visible tooltip.

#### Parameters

`widget`
:   *Type:* `GtkWidget`

    The `GtkWidget` you want to set a `tooltip` and/or `help_id` for.

    |  |
    | --- |
    | The data is owned by the caller of the function. |

`tooltip`
:   *Type:* `const gchar*`

    The text for this widget’s tooltip (or `NULL`).

    |  |
    | --- |
    | The argument can be `NULL`. |
    | The data is owned by the caller of the function. |
    | The value is a NUL terminated UTF-8 string. |

`help_id`
:   *Type:* `const gchar*`

    The `help_id` for the `GtkTipsQuery` tooltips inspector.

    |  |
    | --- |
    | The data is owned by the caller of the function. |
    | The value is a NUL terminated UTF-8 string. |
