![](../images/gimp-devel-logo.png)

### [GimpUi](index.html)

API Version: 3.0

Library Version: 3.0.8

##### Type

[ImageComboBox](class.ImageComboBox.html)

##### Constructors

[new](ctor.ImageComboBox.new.html)

Generated by [gi-docgen](https://gitlab.gnome.org/GNOME/gi-docgen) 2023.1

### Constructor

# [GimpUi](index.html)[ImageComboBox](class.ImageComboBox.html)new

since: 2.2

#### Declaration [[src]](https://gitlab.gnome.org/GNOME/gimp/-/blob/master/libgimp/gimpimagecombobox.h#L46 "go to source location")

```
```
GtkWidget*
gimp_image_combo_box_new (
  GimpImageConstraintFunc constraint,
  gpointer data,
  GDestroyNotify data_destroy
)
```
```

#### Description [[src]](https://gitlab.gnome.org/GNOME/gimp/-/blob/master/libgimp/gimpimagecombobox.c#L121 "go to source location")

Creates a new `GimpIntComboBox` filled with all currently opened
images. If a `constraint` function is specified, it is called for
each image and only if the function returns `TRUE`, the image is
added to the combobox.

You should use `gimp_int_combo_box_connect()` to initialize and
connect the combo. Use `gimp_int_combo_box_set_active()` to get the
active image ID and `gimp_int_combo_box_get_active()` to retrieve the
ID of the selected image.

Available since: 2.2

#### Parameters

`constraint`
:   *Type:* [`GimpImageConstraintFunc`](callback.ImageConstraintFunc.html)

    ```
                               A `GimpImageConstraintFunc` or `NULL`.
    ```

    |  |
    | --- |
    | The argument can be `NULL`. |

`data`
:   *Type:* `gpointer`

    A pointer that is passed to `constraint`.

    |  |
    | --- |
    | The argument can be `NULL`. |
    | The data is owned by the caller of the function. |

`data_destroy`
:   *Type:* `GDestroyNotify`

    Destroy function for `data`.

#### Return value

*Type:* `GtkWidget`

A new `GimpIntComboBox`.

|  |
| --- |
| The data is owned by the called function. |
