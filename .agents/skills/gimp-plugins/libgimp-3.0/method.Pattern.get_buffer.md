![](../images/gimp-devel-logo.png)

### [Gimp](index.html)

API Version: 3.0

Library Version: 3.0.8

##### Type

[Pattern](class.Pattern.html)

##### Methods

[get\_buffer](method.Pattern.get_buffer.html)
[get\_info](method.Pattern.get_info.html)

Generated by [gi-docgen](https://gitlab.gnome.org/GNOME/gi-docgen) 2023.1

### Method

# [Gimp](index.html)[Pattern](class.Pattern.html)get\_buffer

#### Declaration [[src]](https://gitlab.gnome.org/GNOME/gimp/-/blob/master/libgimp/gimppattern.h#L42 "go to source location")

```
```
GeglBuffer*
gimp_pattern_get_buffer (
  GimpPattern* pattern,
  gint max_width,
  gint max_height,
  const Babl* format
)
```
```

#### Description [[src]](https://gitlab.gnome.org/GNOME/gimp/-/blob/master/libgimp/gimppattern.c#L69 "go to source location")

Gets pixel data of the pattern within the bounding box specified by `max_width`
and `max_height`. The data will be scaled down so that it fits within this
size without changing its ratio. If the pattern is smaller than this size to
begin with, it will not be scaled up.

If `max_width` or `max_height` are 0, the buffer is returned in the pattern’s
native size.

Make sure you called `gegl_init()` before calling any function using
`GEGL`.

#### Parameters

`max_width`
:   *Type:* `gint`

    A maximum width for the returned buffer.

`max_height`
:   *Type:* `gint`

    A maximum height for the returned buffer.

`format`
:   *Type:* `Babl`

    An optional Babl format.

    |  |
    | --- |
    | The data is owned by the caller of the function. |

#### Return value

*Type:* `GeglBuffer`

A `GeglBuffer`.

|  |
| --- |
| The caller of the method takes ownership of the data, and is responsible for freeing it. |
