![](../images/gimp-devel-logo.png)

### [Gimp](index.html)

API Version: 3.0

Library Version: 3.0.8

##### Type

[TextLayer](class.TextLayer.html)

##### Constructors

[new](ctor.TextLayer.new.html)

Generated by [gi-docgen](https://gitlab.gnome.org/GNOME/gi-docgen) 2023.1

### Constructor

# [Gimp](index.html)[TextLayer](class.TextLayer.html)new

since: 2.6

#### Declaration [[src]](https://gitlab.gnome.org/GNOME/gimp/-/blob/master/libgimp/gimptextlayer_pdb.h#L35 "go to source location")

```
```
GimpTextLayer*
gimp_text_layer_new (
  GimpImage* image,
  const gchar* text,
  GimpFont* font,
  gdouble size,
  GimpUnit* unit
)
```
```

#### Description [[src]](https://gitlab.gnome.org/GNOME/gimp/-/blob/master/libgimp/gimptextlayer_pdb.c#L39 "go to source location")

Creates a new text layer.

This procedure creates a new text layer displaying the specified
`text`. By default the width and height of the layer will be
determined by the `text` contents, the `font`, `size` and `unit`.

The new layer still needs to be added to the image as this is not
automatic. Add the new layer with the [`gimp_image_insert_layer()`](method.Image.insert_layer.html) method.

The arguments are kept as simple as necessary for the basic case.
All text attributes, however, can be modified with the appropriate
`gimp_text_layer_set_*()` procedures.

Available since: 2.6

#### Parameters

`image`
:   *Type:* [`GimpImage`](class.Image.html)

    The image.

    |  |
    | --- |
    | The data is owned by the caller of the function. |

`text`
:   *Type:* `const gchar*`

    The text to generate (in UTF-8 encoding).

    |  |
    | --- |
    | The data is owned by the caller of the function. |
    | The value is a NUL terminated UTF-8 string. |

`font`
:   *Type:* [`GimpFont`](class.Font.html)

    The font to write the text with.

    |  |
    | --- |
    | The data is owned by the caller of the function. |

`size`
:   *Type:* `gdouble`

    The size of text in either pixels or points.

`unit`
:   *Type:* [`GimpUnit`](class.Unit.html)

    The units of specified size.

    |  |
    | --- |
    | The data is owned by the caller of the function. |

#### Return value

*Type:* [`GimpTextLayer`](class.TextLayer.html)

```
     The new text layer. The object belongs to libgimp and you should not free it.
```

|  |
| --- |
| The data is owned by the called function. |
