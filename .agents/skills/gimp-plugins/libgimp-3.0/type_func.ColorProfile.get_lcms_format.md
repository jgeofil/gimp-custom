![](../images/gimp-devel-logo.png)

### [Gimp](index.html)

API Version: 3.0

Library Version: 3.0.8

##### Type

[ColorProfile](class.ColorProfile.html)

##### Functions

[get\_lcms\_format](type_func.ColorProfile.get_lcms_format.html)

Generated by [gi-docgen](https://gitlab.gnome.org/GNOME/gi-docgen) 2023.1

### Function

# [Gimp](index.html)[ColorProfile](class.ColorProfile.html)get\_lcms\_format

since: 2.10

#### Declaration [[src]](https://gitlab.gnome.org/GNOME/gimp/-/blob/master/libgimpcolor/gimpcolorprofile.h#L94 "go to source location")

```
```
const Babl*
gimp_color_profile_get_lcms_format (
  const Babl* format,
  guint32* lcms_format
)
```
```

#### Description [[src]](https://gitlab.gnome.org/GNOME/gimp/-/blob/master/libgimpcolor/gimpcolorprofile.c#L1569 "go to source location")

This function takes a `Babl` format and returns the lcms format to
be used with that `format`. It also returns a `Babl` format to be
used instead of the passed `format`, which usually is the same as
`format`, unless lcms doesn’t support `format`.

Note that this function currently only supports RGB, RGBA, R’G’B’,
R’G’B’A, Y, YA, Y’, Y’A and the cairo-RGB24 and cairo-ARGB32 formats.

Available since: 2.10

#### Parameters

`format`
:   *Type:* `Babl`

    A `Babl` format.

    |  |
    | --- |
    | The data is owned by the caller of the function. |

`lcms_format`
:   *Type:* `guint32*`

    Return location for an lcms format.

    |  |
    | --- |
    | The argument will be set by the function. |

#### Return value

*Type:* `Babl`

The `Babl` format to be used instead of `format`, or `NULL`
if the passed `format` is not supported at all.

|  |
| --- |
| The data is owned by the called function. |
| The return value can be `NULL`. |
