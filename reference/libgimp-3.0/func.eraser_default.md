![](../images/gimp-devel-logo.png)

### [Gimp](index.html)

API Version: 3.0

Library Version: 3.0.8

##### Functions

Generated by [gi-docgen](https://gitlab.gnome.org/GNOME/gi-docgen) 2023.1

### Function

# [Gimp](index.html)eraser\_default

#### Declaration [[src]](https://gitlab.gnome.org/GNOME/gimp/-/blob/master/libgimp/gimppainttools_pdb.h#L74 "go to source location")

```
```
gboolean
gimp_eraser_default (
  GimpDrawable* drawable,
  gsize num_strokes,
  const gdouble* strokes
)
```
```

#### Description [[src]](https://gitlab.gnome.org/GNOME/gimp/-/blob/master/libgimp/gimppainttools_pdb.c#L482 "go to source location")

Erase using the current brush.

This tool erases using the current brush mask. This function
performs exactly the same as the `gimp_eraser()` function except that
the tools arguments are obtained from the eraser option dialog. It
this dialog has not been activated then the dialogs default values
will be used.

#### Parameters

`drawable`
:   *Type:* [`GimpDrawable`](class.Drawable.html)

    The affected drawable.

    |  |
    | --- |
    | The data is owned by the caller of the function. |

`num_strokes`
:   *Type:* `gsize`

    Number of stroke control points (count each coordinate as 2 points).

`strokes`
:   *Type:* An array of `double`

    Array of stroke coordinates: { s1.x, s1.y, s2.x, s2.y, …, sn.x, sn.y }.

    |  |
    | --- |
    | The length of the array is specified in the `num_strokes` argument. |
    | The data is owned by the caller of the function. |

#### Return value

*Type:* `gboolean`

TRUE on success.
