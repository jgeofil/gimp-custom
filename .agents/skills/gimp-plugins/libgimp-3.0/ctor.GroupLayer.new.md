![](../images/gimp-devel-logo.png)

### [Gimp](index.html)

API Version: 3.0

Library Version: 3.0.8

##### Type

[GroupLayer](class.GroupLayer.html)

##### Constructors

[new](ctor.GroupLayer.new.html)

Generated by [gi-docgen](https://gitlab.gnome.org/GNOME/gi-docgen) 2023.1

### Constructor

# [Gimp](index.html)[GroupLayer](class.GroupLayer.html)new

since: 2.8

#### Declaration [[src]](https://gitlab.gnome.org/GNOME/gimp/-/blob/master/libgimp/gimpgrouplayer_pdb.h#L35 "go to source location")

```
```
GimpGroupLayer*
gimp_group_layer_new (
  GimpImage* image,
  const gchar* name
)
```
```

#### Description [[src]](https://gitlab.gnome.org/GNOME/gimp/-/blob/master/libgimp/gimpgrouplayer_pdb.c#L39 "go to source location")

Create a new group layer.

This procedure creates a new group layer with a given `name`. If
`name` is `NULL`, GIMP will choose a name using its default layer name algorithm.

The new group layer still needs to be added to the image, as this is
not automatic. Add the new layer with the
[`gimp_image_insert_layer()`](method.Image.insert_layer.html) method.
Other attributes such as layer mask, modes and offsets should be set
with explicit procedure calls.

Other procedures useful with group layers:
[`gimp_image_reorder_item()`](method.Image.reorder_item.html), [`gimp_item_get_parent()`](method.Item.get_parent.html),
[`gimp_item_get_children()`](method.Item.get_children.html), [`gimp_item_is_group()`](method.Item.is_group.html).

Available since: 2.8

#### Parameters

`image`
:   *Type:* [`GimpImage`](class.Image.html)

    The image to which to add the group layer.

    |  |
    | --- |
    | The data is owned by the caller of the function. |

`name`
:   *Type:* `const gchar*`

    The group layer name.

    |  |
    | --- |
    | The argument can be `NULL`. |
    | The data is owned by the caller of the function. |
    | The value is a NUL terminated UTF-8 string. |

#### Return value

*Type:* [`GimpGroupLayer`](class.GroupLayer.html)

The newly created group layer.

|  |
| --- |
| The data is owned by the called function. |
