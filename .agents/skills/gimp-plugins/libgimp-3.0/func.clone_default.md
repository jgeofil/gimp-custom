![](../images/gimp-devel-logo.png)

### [Gimp](index.html)

API Version: 3.0

Library Version: 3.0.8

##### Functions

Generated by [gi-docgen](https://gitlab.gnome.org/GNOME/gi-docgen) 2023.1

### Function

# [Gimp](index.html)clone\_default

#### Declaration [[src]](https://gitlab.gnome.org/GNOME/gimp/-/blob/master/libgimp/gimppainttools_pdb.h#L49 "go to source location")

```
```
gboolean
gimp_clone_default (
  GimpDrawable* drawable,
  gsize num_strokes,
  const gdouble* strokes
)
```
```

#### Description [[src]](https://gitlab.gnome.org/GNOME/gimp/-/blob/master/libgimp/gimppainttools_pdb.c#L196 "go to source location")

Clone from the source to the dest drawable using the current brush

This tool clones (copies) from the source drawable starting at the
specified source coordinates to the dest drawable. This function
performs exactly the same as the `gimp_clone()` function except that
the tools arguments are obtained from the clones option dialog. It
this dialog has not been activated then the dialogs default values
will be used.

#### Parameters

`drawable`
:   *Type:* [`GimpDrawable`](class.Drawable.html)

    The affected drawable.

    |  |
    | --- |
    | The data is owned by the caller of the function. |

`num_strokes`
:   *Type:* `gsize`

    Number of stroke control points (count each coordinate as 2 points).

`strokes`
:   *Type:* An array of `double`

    Array of stroke coordinates: { s1.x, s1.y, s2.x, s2.y, …, sn.x, sn.y }.

    |  |
    | --- |
    | The length of the array is specified in the `num_strokes` argument. |
    | The data is owned by the caller of the function. |

#### Return value

*Type:* `gboolean`

TRUE on success.
