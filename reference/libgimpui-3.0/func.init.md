![](../images/gimp-devel-logo.png)

### [GimpUi](index.html)

API Version: 3.0

Library Version: 3.0.8

##### Functions

Generated by [gi-docgen](https://gitlab.gnome.org/GNOME/gi-docgen) 2023.1

### Function

# [GimpUi](index.html)init

#### Declaration [[src]](https://gitlab.gnome.org/GNOME/gimp/-/blob/master/libgimp/gimpui.h#L59 "go to source location")

```
```
void
gimp_ui_init (
  const gchar* prog_name
)
```
```

#### Description [[src]](https://gitlab.gnome.org/GNOME/gimp/-/blob/master/libgimp/gimpui.c#L88 "go to source location")

This function initializes GTK with `gtk_init()`, as well as GEGL and babl.

It also sets up various other things so that the plug-in user looks
and behaves like the GIMP core. This includes selecting the GTK theme
and setting up the help system as chosen in GIMP preferences. Any
plug-in that provides a user interface should call this function.

It can safely be called more than once. Calls after the first return
quickly with no effect.

#### Parameters

`prog_name`
:   *Type:* `const gchar*`

    The name of the plug-in which will be passed as argv[0] to
    `gtk_init()`. It’s a convention to use the name of the
    executable and *not* the PDB procedure name.

    |  |
    | --- |
    | The data is owned by the caller of the function. |
    | The value is a NUL terminated UTF-8 string. |
